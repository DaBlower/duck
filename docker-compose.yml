services:
  app:
    build: .
    container_name: obobs-duck
    environment:
      - DOCKER_RUN=true
    volumes:
      - ./db:/app/db
      - ./logs:/app/logs

    env_file:
      - .env

    restart: unless-stopped